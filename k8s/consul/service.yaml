apiVersion: v1
kind: Service

metadata:
  name: consul

spec:
  selector:
    app: consul  # v1 uses a flat selector

  ports:
    - port: 8500
      targetPort: 8500
      nodePort: 30150

  type: NodePort

